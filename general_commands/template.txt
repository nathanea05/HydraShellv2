# Library Imports


# Local Imports
from core.command import Command
from core.session import Session
from repl.parse_command import ParsedCommand


def _cmd(session: Session, parsed_command: ParsedCommand):
    """Docstring for cmd function"""


class Cmd(Command):
    """Docstring for Cmd"""
    name = ""
    description = ""
    args = None
    kwargs = None
    help = ""

    def execute(self, session: Session, parsed_command: ParsedCommand):
        _cmd(session, parsed_command)
        return